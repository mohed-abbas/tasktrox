# ===========================================
# SHARED INFRASTRUCTURE — ENVIRONMENT
# ===========================================
# Copy to /home/murx/shared/.env.infra on VPS
# Used by docker-compose.infra.yml
# NEVER commit this file with real values.

# PostgreSQL superuser — shared across all projects
# Generate password with: openssl rand -base64 32
POSTGRES_USER=dbadmin
POSTGRES_PASSWORD=<STRONG_RANDOM_PASSWORD>
