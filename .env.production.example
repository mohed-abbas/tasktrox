# ===========================================
# TASKTROX - PRODUCTION ENVIRONMENT TEMPLATE
# ===========================================
# Copy this file and fill in real values for production deployment.
# NEVER commit actual production secrets to version control!
#
# For Render.com: Set these in Dashboard > Your Service > Environment
# For Vercel: Set NEXT_PUBLIC_* vars in Dashboard > Project > Settings > Environment Variables

# =====================
# BACKEND ENVIRONMENT
# =====================
# (Set these in Render.com dashboard)

# ----- Core -----
NODE_ENV=production
PORT=4000

# ----- Database (Neon PostgreSQL) -----
# Get from: console.neon.tech > Your Project > Connection Details
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# ----- Cache (Upstash Redis) -----
# Get from: console.upstash.com > Your Database > Details
# Format: rediss://default:password@host:6379 (note: rediss with double 's' for TLS)
REDIS_URL=rediss://default:YOUR_PASSWORD@your-redis.upstash.io:6379

# ----- JWT Authentication -----
# Generate secure secrets with: openssl rand -base64 48
JWT_SECRET=your-64-character-secure-random-string-here
JWT_REFRESH_SECRET=another-64-character-secure-random-string-here
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ----- URLs -----
# Update these after deploying to Vercel and Render
# CORS_ORIGIN should match your Vercel frontend URL exactly
CORS_ORIGIN=https://your-app.vercel.app
FRONTEND_URL=https://your-app.vercel.app
APP_URL=https://your-app.vercel.app
API_URL=https://your-api.onrender.com

# ----- Rate Limiting -----
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX_REQUESTS=5

# ----- OAuth (Optional) -----
# Only needed if enabling social login
# Google: console.cloud.google.com > APIs & Services > Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=https://your-api.onrender.com/api/v1/auth/google/callback

# Apple: developer.apple.com (requires paid account)
APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=
APPLE_CALLBACK_URL=https://your-api.onrender.com/api/v1/auth/apple/callback


# =====================
# FRONTEND ENVIRONMENT
# =====================
# (Set these in Vercel dashboard)

# ----- API Connection -----
# Your Render backend URL (get after deploying backend)
NEXT_PUBLIC_API_URL=https://your-api.onrender.com/api/v1

# ----- Features -----
# Set to "true" to show OAuth login buttons
NEXT_PUBLIC_ENABLE_OAUTH=false

# ----- Analytics (Optional) -----
# Google Analytics measurement ID
NEXT_PUBLIC_GA_ID=
